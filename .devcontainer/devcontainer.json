{
  "name": "coreboot-dev",
  "build": {
    "dockerfile": "./Dockerfile",
    "context": "..",
    "options": [
      "--network=host"
    ],
    "args": {
      "HOST_UID": "1000"
    }
  },
  "runArgs": [
    "--network=host"
  ],
  "remoteUser": "coreboot"
}